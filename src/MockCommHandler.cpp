/* MockCommHandler implementation intentionally removed.
 * The mock handler is not part of this build. If you need mock
 * functionality for testing, reintroduce a separate testing-only
 * module rather than keeping mocks in production firmware.
 */

